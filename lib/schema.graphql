type Course {
  _id: ID!
  title: String!
  teacher: String
  description: String!
  topic: String
}

type Query {
  "devuelve los cursos"
  getCourses: [Course]
  "devuelve un curso"
  getCourse(id: ID!): Course
}
